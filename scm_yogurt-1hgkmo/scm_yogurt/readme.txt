Before executing the ShinyR app.R file, open it.

Edit the filepath to requirements.txt, so that relevant dependencies can be installed into the virtual environment. 
The installation of dependencies is automatically performed when executing the app.R file)